<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) : %msg %n
            </pattern>
        </encoder>
    </appender>
    <logger name="org.hibernate" level="ERROR"/>
    <logger name="org.springframework.messaging" level="ERROR"/>
    <!-- Temporary: surfaces the real Lettuce/TLS handshake error instead of a silent hang. Revert once diagnosed. -->
    <logger name="io.lettuce.core" level="DEBUG"/>
    <logger name="org.springframework.data.redis" level="DEBUG"/>
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>